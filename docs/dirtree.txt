emblocs/
    .gitignore
    emblocs.json       <-- obsolete?
    emblocs_cfg.json
    LICENSE
    README.md
    docs/
        architecture.md
        bloc_language.md
        blocs_language.md
        DEVELOPMENT.md
        dirtree.txt  <-- this file
        monitor-startup.md
        monitor.md
    cmake/
        emblocs.cmake    <-- included from master CMakeLists.txt
    src/
        components/
            *.bloc
            *.c
            *.h
        emblocs/
            CMakeLists.txt  <-- not sure what's in here
            ebl_api.h          <-- old headers for old API
            ebl_common.h       <-- headers for types and such
            ebl_comp.h         <-- headers for component definitions
            ebl_config.h       <-- template config file (copy to project)
            ebl_core.h         <-- old headers for old API
            ebl_monitor.h      <-- header for new monitor
            ebl_platform.h     <-- old approach for platform independence
            ebl_priv.h         <-- old header for internal types, etc
            ebl_core.c         <-- roughly equivalent to emblocs.py - main structs
            ebl_monitor.c      <-- new monitor
            ebl_parse.c        <-- parser for a very different dialect of what became .blocs
            ebl_show.c         <-- roughly equivalent to the describe() methods
        misc/
            some utlilty libs used by emblocs/
            CMakeLists.txt  <-- not sure what's in here
            bundle.c & .h      (serial communications library)
            linked_list.c & .h (linked list management code)
            printing.c & .h    (stripped down printf-like for embedded)
            ser_crc.c & .h     (old CRC code for serial.c)
            serial.c & .h      (old predecessor of bundle.c)
            str_to_xx.c & .h   (conversion from string to float, u32, s32)
            target_hooks.h     (default version of portability code)
        old_components/
            old, .c only components
    python/
        bl_xxxx.py       <- four files related to my GUI efforts
        emblocs_gui.py   <- also from my GUI efforts
        testing.py       <- test script from before pytest
        bloc_compiler.py
        bloc_parser.py
        bloc_resolver.py
        blocs_compiler.py
        blocs_parser.py
        emblocs.py
        enblocs_output.py
        enblocs_common.py
        expressions.py
        parse_common.py
        tests/          <- new test infrastructure using pytest
            test_xxx.py <- approximately one per .py file in python/
            data/
                bad/
                    sample files with various errors
                good/
                    sample files that work
                tmp/        <- scratch directory, not tracked
                    files created by blocs_parser.py tests
